node_modules/
.pnpm-store/
dist/
web/dist/
web/public/data/
# 本地调试用 mock 角色数据（不入库）
web/public/mock_*.d4e.json
*.tsbuildinfo
*.log
.DS_Store
# 审计报告（派生产物，不入库）
out/categories/_audit.json
# 取色模式对比探针（本机工具，含写死的绝对路径与派生产物，不入库）
out/color-variant-probe/

# DSH vision-router artifacts (local tooling output)
.dsh-vision-router/
# npm cache (local)
.npm-cache/
# pnpm 元数据缓存（本地）
.pnpm-cache/

# Electron / electron-builder 下载缓存（本地）
.electron-cache/
.electron-builder-cache/

# 桌面版内置字体（由 desktop/scripts/fetch-fonts.mjs 抓取，不入库）
desktop/assets/fonts/

# 桌面端派生产物（由 web 构建生成，不入库）
desktop/renderer/
desktop/release/
# 开发用说明文档：仅本地保留、不入库（维护者要求移除）
/私设模块.md
/私设编辑器设计.md
/私设实现自动执行提示词.md
/私设设计/
