# 自动执行提示词：私设条目编辑器全类型实现

> 使用方式：把本文件完整内容复制到新的对话（同一工作区 j:\4E-NEXT），或在新对话中直接说「按 j:\4E-NEXT\私设实现自动执行提示词.md 执行，完成后生成实现报告」。
> 执行者须知：这是一个无人值守的自动化实现任务，完成后用户会按设计文档逐类核查。

---

## 角色与目标

你是 D&D 4E 私设条目编辑器的自动化实现工程师。项目已有一份主设计文档和 18 份逐类设计文档（共 19 类私设条目）。**威能（power）类型是已完成标杆**，你的任务是把其余 18 类按设计文档实现到同等程度的结构化编辑，并保证现有功能不回归。

## 项目信息

- 工作目录：`j:\4E-NEXT`（pnpm monorepo；根目录 `src/` 是数据管线，**只允许改 `web/` 前端，禁止动数据管线**）
- 前端技术栈：React + Vite + TypeScript（`web/`）
- 启动开发服务器：`pnpm --filter 4enext-web dev`（Vite，端口 5173 起，被占自动顺延；用浏览器验证时以实际端口为准）
- 类型检查：`pnpm --filter 4enext-web typecheck`（等价 `tsc --noEmit`）
- 官方数据语料（设计统计依据、逆解析验证源）：`web/dist/data/categories/*.json`
- 环境：Windows + PowerShell。若 pnpm/node 命令报「sandbox 权限/文件系统」类错误，重试时禁用沙箱执行

## 关键文件

| 文件 | 作用 |
| --- | --- |
| `j:\4E-NEXT\私设编辑器设计.md` | 主设计文档：六条设计哲学、power 标杆全量设计、**可复用资产清单**、实施路线图、状态追踪表 |
| `j:\4E-NEXT\私设设计\01-18-*.md` | 18 类逐类设计文档（每类含：卡片结构/数据模型/设计要点/解析方式/实现清单/开放问题），**按实现清单逐项勾选** |
| `web/src/homebrew/EntryEditor.tsx` | 编辑器主体（表单左栏 + 预览右栏；主次分明分区渲染） |
| `web/src/lib/homebrewSchema.ts` | schema 驱动：CATEGORY_FIELDS / CATEGORY_SECTIONS / POWER_PRESETS / serialize·parse 函数 |
| `web/src/sheet/EntryCard.tsx` | 预览卡片（PowerCard/ItemCard/FeatCard/GenericCard） |
| `web/src/styles.extra.css` | 增补样式（新增样式一律写这里，走 `--md-sys-color-*` 语义色与 `--fw-*` 字重） |

## 硬性约束（违反即返工）

1. **只能在 feature 分支开发**（如 `feat/homebrew-editor-all`），禁止直接改 main；**禁止 push**；完成一个类型提交一次（`git commit`，信息含类型名，如 `feat(homebrew): 装备威能段编辑器`）
2. pnpm 版本须为 package.json 指定的 11.21.0；若版本检查报错设 `pnpm_config_pm_on_fail=ignore`
3. **不得改动 power（威能）编辑器现有行为**（预设条、标签块、次攻击组、附加设置折叠）；如确需改，报告中说明
4. **不过度设计**：只按各文档「实现清单」实现；文档没写的功能不要加
5. 每完成一个类型必须 `typecheck` 通过，不留语法/类型错误
6. 临时统计/验证脚本（.mjs，node 执行）用完即删
7. 可读 `TRAE-browseruse` 技能做浏览器验证；复杂运行时 bug 可用 `TRAE-debugger`

## 实施顺序（先通用组件，后逐类）

### 阶段 1：通用能力建设（先建好，后续类型复用）

1. **通用「威能引用编辑器」**：从段落提取 `{{威能名}}`/`[[威能]]` 引用 → 分组列表编辑 → 回写正文。服务于 theme/domain/bloodline/pact/magic-school/class/paragon-path/epic-destiny/race（见各文档「设计要点」）
2. **`LevelFeatureSections` 等级特性小节组件**：`N级：标题 + 正文 + 可选威能引用`（服务于 epic-destiny/paragon-path/class）
3. **PowerBlockEditor 复用扩展**：装备「威能段」（段头=关键词✦频率（动作）+ 正文标签块）、生物「行动/特质段」、套装「件数增益块」
4. **各类型 parse/serialize 解析器**（按各文档「解析方式」）：装备威能段、仪式头部六行、专长关联威能等级表、生物数据块（`div.creature` 双栏行 + `bg-power` 段）

### 阶段 2：逐类实现（按 私设设计/ 编号，简单→复杂）

对每类执行固定流程：
1. 通读该类型设计文档
2. 按其「实现清单」逐项实现，每完成一项把文档 checkbox `[ ]` 改为 `[x]`
3. 官方格式解析类需求，以 `web/dist/data/categories/{类型}.json` 实测数据为准；可用一次性 node 脚本统计/验证，用完删除
4. 浏览器验证编辑与预览（见验证协议）
5. 提交一次

## 验证协议（每类必做）

1. `pnpm --filter 4enext-web typecheck` 通过
2. 浏览器（TRAE-browseruse）验证：创建该类型条目 → 字段面板按「主次分明」渲染（core 常驻、其余在附加设置折叠）→ 结构化编辑可用 → 右侧预览正确 → 保存后重新打开可回填（双向序列化）
3. 回归：power 威能编辑器仍正常（预设条、标签块、次攻击组、附加设置折叠）
4. 主次分明布局已就绪：`CATEGORY_SECTIONS` 各类型已标 core，`EntryEditor` 已按 core/extra 分流——你的改动须保持该机制

## 报告要求（次日核查依据）

1. 每完成一类：更新其文档「实现清单」checkbox + 主文档「状态追踪」表
2. 全部完成后生成 `j:\4E-NEXT\私设设计\实现报告.md`，按类型列出：完成 / 部分 / 未做，遇到的技术问题、与文档不一致之处、遗留 TODO、待用户确认的决策

## 兜底策略

- 某类型卡住（信息不足/规则冲突）：不要硬做，在《实现报告.md》记录卡点与原因，跳下一类型继续
- 无法浏览器验证时：至少 typecheck 通过 + 对该类型序列化函数做数据级自测（用官方 json 样本逆解析再序列化比对）
- 设计文档与现有代码冲突时：以现有代码既定模式为准，冲突点记入报告
- 全程不得中断整体流程；遇到单个文件反复失败超过 3 次，改走兜底，避免无限重试

## 完成标准

- 18 类全部按清单实现（或清单中明确标注未做项与原因）
- typecheck 通过，power 回归通过
- 所有文档 checkbox 状态反映真实实现
- 《实现报告.md》存在且覆盖全部类型
