# 怪物数据管线（怪物手册 1~3 合订本 → 规范 JSON）

面向未来的「主持模式」做的一次数据来源可行性验证：把社区整理的大集合式怪物数据
（`DnD4E-怪物手册1~3合订本（六巫翻译-灵霜整理）.xlsx`）按玩家资源管线的同一套分层方式
解析、切块、规范化，并接入可重复运行、可增量同步的管线。

> 本管线的 `monster` 与 web 端的 `creature`（私设生物）是两套不同的东西，约定见第 6 节，接入前请先读那一节。

- 管线代码：`src/etl/monsters/`（`extract` → `segment` → `parse` → `index` + `audit`）
- 来源读取：`src/lib/xlsx.ts`（无第三方依赖的 XLSX 只读层）
- 契约：`src/schema/monster.ts`

---

## 1. 来源长什么样

工作簿有四个工作表，各司其职：

| 工作表 | 规模 | 作用 |
| --- | --- | --- |
| 计数 | 35 行 | 等级分布统计（Lv1..Lv35） |
| 总表 | 918 行 × 8 列 | 怪物索引：等级 / 名称 / 类型 / 职能 / 界域 / 生物类别 / 生物种群 / 原文 |
| 目录 | 89 行 × 3 列 | MM1 / MM2 / MM3 三本书的章节清单（65 / 85 / 88 条） |
| 数据 | 14802 行 × 50 列 | 正文：逐行线性排布的大集合文本，另有 418 张内嵌插图 |

「数据」表没有表格结构，它是一段一段贴上去的正文流：

- 章节标题行：`天使ANGEL`、`巨蜥怪 BASILISK`；
- 怪物标题行：第一行是 `名称（English Name）` + 尾部 `LV14 护卫`，第二行是 `中型 星界 类人生物（天使）` + 尾部 `XP1000`；
- 数值行：`HP141；重伤 70`、`AC30 强韧 26 反射 24 意志 25`、`速度 6，飞行 8（盘旋）`；
- 段头行：`特性` / `标准动作` / `移动动作` / `次要动作` / `自由动作` / `触发动作`；
- 威能行：`基本近战：巨剑（光耀，武器）·随意` + 正文行；
- 图注行：`守护天使，图片出自 4E《怪物图鉴》`；译注行：`（译注：…）`。

排版有两个世代，抽取规则必须同时兼容：

- **早期体例**（怪物图鉴 1/2/3）：段头给动作类别，威能头以 `·频率` 结尾，正文另起一行以 `攻击：` 开头；
- **精华版体例**：没有段头，动作与频率写进威能名的括注（`基本近战：触手打击（标准动作；随意）`），正文形如 `触及 5；+39vs.AC；3d8+8 伤害。`。

### 排版的四个坑（都已在管线里处理）

1. **右侧栏位漂移**：LV/XP、先攻/侦查、免疫/抗性在不同章节放在不同列（R / Z / AF / AH / I / K…）。
   合并规则是「按列序 + 行内按行号对齐」，把同一条视觉行的左右两半拼回一行。
2. **硬换行**：正文按显示宽度断行，长段落的尾巴会单独成行（如 `…最后一个指定它的天使` / `的守护对象。`）。
   不处理就会把碎片当成威能名。
3. **同物理行挤多个字段**：如 `腐烂恶臭（毒素） 灵气 2：…虚弱。 HP34；重伤 17  先攻+2 …`。
   需要按字段边界先拆行。
4. **全角/半角括号混用**：`地精马格奴比耶的侍僧（Goblin Acolyte of Maglubiyet)`。
   配对与名称解析都要两种括号都算。

---

## 2. 分层与产物

沿用玩家资源管线的分层（无损 → 分块/归类 → 规范 → 派生 → 审计）：

| 本管线 | 玩家资源管线对应物 | 说明 |
| --- | --- | --- |
| `out/monsters/raw/rows.jsonl` | `out/raw/tiddlers-raw.jsonl` | 无损层：数据表逐行的每列原文 + 合并文本 |
| `out/monsters/raw/sheets.json` | — | 总表 / 目录 / 计数 / 图片锚点 / 文件前言 |
| `out/monsters/raw/_source.json` | — | 源文件指纹（sha256、字节数、修改时间、工作表部件） |
| `out/monsters/blocks/blocks.jsonl` | `out/categories/_classification.json` | 分块层：preamble / section / monster 三类记录 |
| `out/monsters/canonical/monster.jsonl` | `out/canonical/*.jsonl` | 规范层：一条怪物一个对象 + `provenance.contentHash` |
| `out/monsters/canonical/_meta.json` `_changes.json` | 同名 | 增量同步统计与变更清单 |
| `out/monsters/categories/monster.json` | `out/categories/*.json` | 派生层：前端可直接加载的紧凑全量数组 |
| `out/monsters/index/monsters.json` `manifest.json` | `out/index/*` | 检索索引与分面统计（按书/等级/类型/职能/界域/类别/种群） |
| `out/monsters/tiddlers/{MM,MM2,MM3}/*.json` | — | 展示态：4E Wiki creature 词条，见第 6 节 |
| `out/monsters/audit/_coverage.json` | `out/categories/_audit.json` | 审计：字段覆盖率、对不上的地方（**不入库**） |

图片：规范层记录 `image.part`（工作簿内部件路径，如 `xl/media/image204.jpeg`）与图注、
锚点行号，**不导出图片本体**（425 张约 40MB，进仓库会显著推高体积）。需要时按需导出：

```bash
pnpm monsters:media   # → out/monsters/media/（已 gitignore）
```

---

## 3. 命令

```bash
pnpm monsters          # 跑完整条怪物管线（tsc && node dist/cli.js monsters）
pnpm monsters:tiddler  # 展示态：渲染成 4E Wiki 的 creature 词条（web 端生物卡可直接渲染）
pnpm monsters:media    # 按需导出内嵌插图
pnpm all               # 原有全量管线，末尾会带上 monsters（源文件缺失时自动跳过）
```

源文件不随仓库分发（44MB）。管线在**仓库根目录与 `data/`** 里找文件名含「怪物」的 `.xlsx`，
按修改时间取最新的一份——**因此来源更新时直接放新文件即可，不需要改代码**；
只剩一个 xlsx 时也会退回到它。找不到源文件时 `monsters` 命令只提示跳过，
不会让 `pnpm all` 整体失败（与 `rules` 同一处理）。

---

## 4. 来源更新时怎么走

1. 把新版 xlsx 放进仓库根目录或 `data/`（文件名带「怪物」），旧版删掉或改名。
2. `pnpm monsters`。
3. 看输出与 `out/monsters/audit/_coverage.json`：
   - **新增 / 更新 / 移除** 分别多少条；
   - 字段覆盖率有没有掉（掉说明上游排版变了）；
   - `sections.unmatched` 有没有变多（章节改名了）；
   - `index.levelConflicts` / `blocksWithoutIndexRow` 有没有变多（总表与正文对不上了）。
4. 需要人工过一遍时看 `out/monsters/canonical/_changes.json`（首次同步只记统计，之后给全量清单）。
5. 改抽取规则时**必须同时递增** `src/etl/monsters/parse.ts` 里的 `MONSTER_EXTRACTOR_VERSION`：
   `contentHash` 只覆盖原文，抽取器变了而原文没变的话，增量同步会误以为条目没变而沿用旧字段。

增量同步的判定是 `原文哈希相同 && 抽取器版本相同 → 原样沿用上一版对象`，
所以没变的条目字节稳定、git diff 干净；来源里被删掉的条目进 `removed` 清单。

---

## 5. 实测结果（本仓库当前来源）

| 指标 | 数值 |
| --- | --- |
| 数据表非空行 | 14547 |
| 章节 | 239（对齐目录 214 / 未对齐 25） |
| 怪物块 | 927 |
| 规范层条目 | 927（zod 校验全部通过） |
| 书归属 | MM1 304 / MM2 322 / MM3 301 |
| 威能 | 3828 条（10524 个正文小节） |
| 特性 | 554 条 |
| 有插图的条目 | 382 / 927（41.2%） |
| 等级分布 | 1-10 级 435 / 11-20 级 355 / 21-30 级 131 / 31+ 级 6 |

字段抽取覆盖率（占 927 条）：

| 覆盖 ~100% | 覆盖 79%~99% | 覆盖较低（来源本身稀疏） |
| --- | --- | --- |
| 等级、职能、界域、生物类别、HP、AC、强韧/反射/意志、速度、先攻、侦查、能力值、阵营、威能 | 生物种群 99.1%、XP 99.8%、语言 79.5% | 类型（杂兵/精英/…）36.8%、技能 57.1%、装备 45.6%、特性 41.0%、图片 41.2% |

「类型」覆盖率低不是抽取失败：4E 里**标准怪物本来就没有这个标记**，
来源也只有 336/918 行填了该列；「特性」覆盖率低同理——精华版体例没有 `特性` 段头，
被动能力落在威能列表里（`group` 已用动作类别回填）。

### 已知限制 / 待补

- `headOnlyPowers` 33 条、`suspiciousNames` 1 条：威能头与正文切分的残余误差（约占 0.8%）。
  这些条目的 `sourceText` 始终保留，前端可按原文渲染。
- 24 条图片锚点没有落到任何怪物块上（多为整版插图）。
- 章节名与「目录」列有 25 处对不上，逐条列在审计里，都是上游自己的不一致，例如：
  `地精 GOBLIN`（目录作「哥布林」）、`地妖地精 NORKER`（目录作「地妖哥布林」）、
  动物附录的 `熊 BEAR` / `鳄鱼 CROCODILE` 等（目录统一记作 `动物APPENDIX:ANIMALS`）。
  对不上的章节沿用当前位置所属的书，并记 `bookMatched: false`，不会悄悄给错归属。
- 总表与正文并非完全一致（这是上游数据本身的状况，审计会如实报告）：
  - 正文有、总表无 8 条（巢穴工蚁、穆尔塔尔之狱、被遗忘者恐惧骸、罗丝蜘蛛神后、阿尔班之眷族、战斗编织者、泽希尔的盘绕者、泽希尔的蜕皮）；
  - 总表有、正文无 2 条（夸塞魔、火吻蛇蜥）；
  - 等级冲突 12 条（正文的 LV 与总表等级不一致，如 `邦德怪守卫` 正文 LV16 / 总表 28）；
  - 4 条名称写法不同（中英文逗号之别）；
  - 「计数」表与「总表」的等级分布有 7 处对不上。

---

## 6. 与 web 端 `creature`（私设生物）的关系

仓库里现在有**两套「生物」数据**，来源、形状、用途都不同，**不是同一件事**：

| | `creature`（已有） | `monster`（本管线新增） |
| --- | --- | --- |
| 来源 | 4e Wiki 官方词条 + 私设编辑器录入 | 怪物手册 1~3 合订本 xlsx |
| 形状 | `CreatureBlock`：`<div class=creature>` HTML 数据块（头部 + 双栏数据行 + 行动/特质段） | `MonsterEntry`：等级/职能/四防/能力值/威能 parts/插图锚点等扁平字段 |
| 规模 | 205 条 | 927 条 |
| 位置 | `out/categories/creature.json`、`web/src/lib/homebrewSchema.ts` | `out/monsters/**`、`src/schema/monster.ts` |
| 用途 | 私设编辑器读写（CategorySpec 列表里的第 15 类，被排在整个重构的最后一步） | 主持模式的数据来源 |

由此有两条**必须守住的约定**：

1. **不要把 `monster` 登记进 `web/src/data/labels.ts` 的 `CATEGORY_ORDER` / `CATEGORY_LABELS`。**
   那张表驱动私设页的分类管理与统计，塞进一个非私设类别会让私设页多出一个永远为空的分类。
   两者若将来需要互通（让怪物手册的怪物也能当私设编辑），走**显式转换**
   （`MonsterEntry` → `CreatureBlock`），不要合并类别键。
2. **不要用 web 端的共享 `WikiBody` 渲染怪物的 `sourceText`。**
   `sourceText` 是 xlsx 逐行原文（纯文本 + 硬换行 + 列合并留下的连续空格），
   **不是 wikitext**；它只作为溯源与兜底，正常展示应当走结构化字段
   （`hp/ac/fortitude/…`、`powers[].parts`、`abilities`），
   `sourceText` 仅用于「查看原文」这类旁路入口。

### 6.1 展示态：creature 词条导出

`pnpm monsters:tiddler` 会把规范层渲染成 4E Wiki 的 creature 词条（TiddlyWiki tiddler JSON，
写到 `out/monsters/tiddlers/{MM,MM2,MM3}/`）。这样做是因为 web 端**已经有**一整套生物卡：

- `web/src/lib/wikirender.ts` 会解析 `{{!!字段}}` 自转写；
- `web/src/sheet/EntryCard.tsx` 用 `gen-creature-card` 渲染 `sourceText`；
- `styles.css` 里的 `.bg-title / .bg-category / .bg-power / .description` 就是为这套标记写的。

所以展示态用这套形态，前端零改动就能显示；规范层则保持扁平结构供计算使用。渲染器固化了：

- 头部两行用字段自转写（`{{!!title}}` / `{{!!level}}` / `{{!!size}}` …）；
- 双栏数值行按「HP+重伤 / 先攻」「AC+三防 / 侦查」「免疫+抗力+易伤+豁免+行动点 / 感官」「速度」四行排；
- 段头用官方词汇（特性 / 标准动作 / 移动动作 / 次要动作 / 自由动作 / 触发动作）；
- 动作图标 `{{$:/dnd/images/…}}`：近战 meleebasic、近程 close、远程 range、区域 area、灵气 aura，
  另按「充能 N,M」补出 four/five/six；
- 用词归一：来源的「抗性」在展示层渲染为官方 wiki 用的「抗力」（官方 205 条里抗力 26 次、抗性 0 次）。

我们比同源的另一套产出多带：**译注**（277 条）、**数据表行号**与 **upstream** 溯源字段（927 条）。

### 6.2 与同源的另一套产出做过交叉验证

仓库里曾有一份平级项目拆解的 `mm1~3怪物/`（928 个 TiddlyWiki creature 词条，`creator: 灵霜`，
`created 2026-08-31` / `modified 2026-09-01` 全部同一时刻，属一次性批量转换；xlsx 的 Excel
元数据最后修改是 2024-01-30，因此**它是下游渲染产物，不是上游来源**）。验证结论：

- 名称集合只差 1 条（夸塞魔：总表有、数据表无，它补了）；**等级 100% 一致**；首见书归属 **100% 一致**；
- 它有一处静默错误：脓包衍体 XP 记为 3000（原文 `XP300（若为脓包魔造出则为 0）`，正确是 300），
  静态文件没有审计层，这类错误不会暴露——这正是必须保留可再审管线的原因；
- 它丢了译注（0/928）与插图（无 image 字段）；
- 用我们的渲染器与它逐条比对（865 条同名）：
  **段头序列 92.4%、威能条数 89.7%、数值行标签 99.2%、双栏行数 97.5% 一致**；
  图标差异 349 条只差充能骰图标、69 条只差灵气图标（都是它漏的，我方更全），真正的内容差异约 40 条；
  威能条数的差异主要来自它把来源里那行孤立的「触发」当成了威能渲染（我方按段头处理，是正确的）。

该目录已在验证完成后移除；本文记录上述数字，不保留对它的依赖。

---

数据落在 `web/public/data/monsters*.json` 根级，与 `rules.json` 同层；
前端只在显式请求时加载，不会被 `manifest.json` 自动纳入，
因此不会污染词条页与私设页的类别列表。

---

## 7. 对「主持模式」的可行性结论

**可行**。作为一个主持向的数据来源，这份来源能稳定给出：

- 927 条可检索的怪物，带等级 / 职能 / 界域 / 类别 / 种群分面（可直接做筛选器与遭遇构建）；
- 每条完整的战斗数值（HP、重伤、四防、速度、先攻、侦查、免疫 / 抗性 / 易伤、豁免、行动点、六项能力值）；
- 3828 条威能 + 554 条特性，逐条带动作类别、使用频率、关键词，以及拆好的
  `攻击 / 命中 / 效果 / 触发 / 前置` 小节（可直接做「点一下投攻击骰」）；
- 382 张怪物插图与图注；
- 三本书的归属与章节目录（可直接做「按书浏览」）。

缺口主要是**来源本身没有的东西**：没有遭遇组建规则、没有战术建议、没有地形/宝藏表；
这些需要另找来源或由主持模式自己生成，不在本管线的范围内。
